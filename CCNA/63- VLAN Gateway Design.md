
---

# 🧭 **تصميم البوابة الافتراضية في الشبكات المحلية (VLAN Gateway Design)**

---

## 📌 أولاً: ما هي الـ VLAN؟

- الـ **VLAN (Virtual Local Area Network)** تعني "الشبكة المحلية الافتراضية".
    
- تُستخدم لتقسيم الشبكة الكبيرة إلى شبكات أصغر منطقية،  
    بحيث تكون كل مجموعة من الأجهزة في VLAN واحدة كأنها شبكة منفصلة تمامًا.
    

مثلاً 👇

- VLAN 10 → قسم الموارد البشرية
    
- VLAN 20 → قسم المالية
    
- VLAN 30 → قسم التقنية
    

---

## 🌐 ثانياً: ما هي **البوابة الافتراضية (Gateway)**؟

- البوابة الافتراضية هي **العنوان (IP)** الذي تستخدمه الأجهزة داخل VLAN  
    للتواصل مع شبكات أخرى (VLANs أخرى أو الإنترنت).
    
- تُعتبر البوابة هي **نقطة العبور بين الشبكات**.
    

📘 مثال:  
لو جهاز في VLAN 10 يريد التواصل مع جهاز في VLAN 20  
→ لازم يمر عبر **البوابة الافتراضية (Gateway)**.

---

## ⚙️ ثالثاً: أين تُنشأ البوابة الافتراضية؟

هناك أكثر من تصميم ممكن، حسب حجم وهيكل الشبكة 👇

---

### **1. Gateway on Router (Router-on-a-Stick)** 🧩

- يُستخدم **جهاز راوتر واحد** متصل بسويتش عبر منفذ Trunk.
    
- كل VLAN تُعرّف كـ Subinterface على الراوتر.
    
- الراوتر هو الذي يقوم بعملية **التحويل بين VLANs (Inter-VLAN Routing)**.
    

📘 مثال:

```
interface Gig0/0.10
 encapsulation dot1q 10
 ip address 192.168.10.1 255.255.255.0

interface Gig0/0.20
 encapsulation dot1q 20
 ip address 192.168.20.1 255.255.255.0
```

- هنا كل Subinterface يمثل **Gateway** لواحدة من الـ VLANs.
    

🔹 **المميزات:** بسيط وسهل في الشبكات الصغيرة.  
🔹 **العيوب:** الراوتر يصبح نقطة ازدحام (Bottleneck) في السرعات العالية.

---

### **2. Gateway on Layer 3 Switch (Multilayer Switch)** ⚡

- في هذا التصميم، يتم إنشاء الـ VLANs على سويتش من الطبقة الثالثة (**L3 Switch**).
    
- السويتش نفسه يملك واجهات **SVIs (Switch Virtual Interfaces)** تعمل كـ Gateways.
    

📘 مثال:

```
interface vlan 10
 ip address 192.168.10.1 255.255.255.0

interface vlan 20
 ip address 192.168.20.1 255.255.255.0
```

- كل **VLAN Interface (SVI)** تمثل البوابة الخاصة بتلك الـ VLAN.
    
- هذا التصميم أسرع من الراوتر، لأن التحويل يتم داخل السويتش مباشرة (Hardware Switching).
    

🔹 **المميزات:** أداء عالي وسرعة ممتازة.  
🔹 **العيوب:** يحتاج سويتش Layer 3 (أغلى من السويتش العادي).

---

### **3. Distributed Gateway (في الشبكات الحديثة)** ☁️

- في الشبكات الحديثة (خاصة في الـ Data Center أو Cloud)،  
    يتم توزيع الـ Gateways على عدة أجهزة مثل **Cisco ACI** أو **VXLAN Fabric**.
    
- الهدف هو تقليل التأخير (Latency) عبر جعل كل Access Switch  
    يحتوي على Gateway محلي لكل VLAN.
    

🔹 **المميزات:**

- أداء أسرع.
    
- تقليل حركة المرور بين الطبقات.
    
- redundancy عالية.
    

---

## 🔄 رابعاً: العلاقة بين VLAN وGateway

|العنصر|الوظيفة|
|---|---|
|VLAN|شبكة منطقية تفصل المستخدمين|
|Gateway|العنوان الذي يخرج منه المرور خارج VLAN|
|DHCP|يعطي الأجهزة عناوين IP داخل VLAN|
|Router أو L3 Switch|ينفذ الاتصال بين VLANs|

---

## 🧠 خامساً: مثال تطبيقي لتوضيح الفكرة

|القسم|VLAN ID|الشبكة|Gateway|
|---|---|---|---|
|HR|10|192.168.10.0/24|192.168.10.1|
|IT|20|192.168.20.0/24|192.168.20.1|
|Finance|30|192.168.30.0/24|192.168.30.1|

كل VLAN لها Gateway خاص بها،  
وكل الأجهزة داخل VLAN تستخدم نفس الـ Gateway للوصول لباقي الأقسام أو الإنترنت.

---

## 🧩 سادساً: نصائح التصميم الجيد للـ VLAN Gateways

1. 🔒 استخدم **ACLs (قوائم التحكم بالوصول)** بين الـ VLANs لتأمين التواصل.
    
2. 🧠 استخدم **HSRP أو VRRP أو GLBP** لتوفير **بوابة احتياطية (Redundant Gateway)**.
    
3. ⚡ فعّل **Routing على Layer 3 Switch** بدلاً من الراوتر إذا كانت السرعة مطلوبة.
    
4. 📡 اجعل تصميم VLANs منطقيًا حسب الوظيفة أو القسم وليس حسب الموقع.
    
5. 🧱 تجنّب وجود VLAN واحدة تغطي فروعًا بعيدة جغرافيًا.
    

---

هل ترغب أن أضيف لك **رسم تخطيطي بالعربي**  
يوضح أنواع تصميم الـ VLAN Gateway (Router-on-a-stick / L3 Switch / Distributed)?  
سيكون مفيد جدًا للفهم البصري.

[[64- SVI (Switch Virtual Interface)]]