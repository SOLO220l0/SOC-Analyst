
## 🧠 **OSPF LSAs (Link-State Advertisements)**

---

## 🎯 أولاً: ما هي الـ LSAs؟

**LSA (Link-State Advertisement)** هي الرسائل أو البيانات التي **يتبادلها راوترات OSPF** لتخبر بعضها البعض عن حالة الشبكات والروابط في الـ area.

كل راوتر يُصدر LSAs خاصة به، وتُجمع جميع LSAs من كل الراوترات في قاعدة بيانات تُسمّى:

> **Link-State Database (LSDB)**

بعدها، يستخدم OSPF **خوارزمية Dijkstra (SPF)** لحساب أفضل المسارات اعتمادًا على المعلومات داخل الـ LSDB.

---

## 🧩 ثانيًا: العلاقة بين LSA – LSDB – SPF – Routing Table

|المرحلة|الاسم|الوظيفة|
|---|---|---|
|1️⃣|LSA|الرسائل التي تحمل معلومات الروابط|
|2️⃣|LSDB|قاعدة بيانات تجمع كل الـ LSAs في الـ area|
|3️⃣|SPF Algorithm|خوارزمية تحسب أقصر المسارات|
|4️⃣|Routing Table|النتيجة النهائية لمسارات OSPF المختارة|

---

## 🔹 ثالثًا: أنواع الـ LSAs (الأساسية)

في OSPFv2 (IPv4) هناك **11 نوع LSA**، لكن الأكثر استخدامًا في CCNA وعمليًا هي أول 5 أنواع 👇

|النوع|الاسم|من يصدره|المدى (يصل إلى أين)|الوظيفة|
|---|---|---|---|---|
|**Type 1**|Router LSA|كل راوتر داخل الـ Area|داخل نفس الـ Area فقط|يعلن عن الروابط المتصلة بالراوتر (واجهاته وخصائصها)|
|**Type 2**|Network LSA|الـ DR (Designated Router)|داخل نفس الـ Area فقط|يعلن عن الشبكات متعددة الوصول (مثل Ethernet)|
|**Type 3**|Summary LSA|ABR (Area Border Router)|من Area إلى Area أخرى|يلخص الشبكات داخل منطقة ويعلنها للمناطق الأخرى|
|**Type 4**|ASBR Summary LSA|ABR|من Area إلى Area أخرى|يعلن عن موقع الراوترات الـ ASBRs|
|**Type 5**|External LSA|ASBR (Autonomous System Boundary Router)|إلى كل الـ Areas (عدا Stub)|يعلن عن الشبكات الخارجية (مثل من BGP أو Static Routes)|

---

## 🧩 أمثلة سريعة توضحها عمليًا:

### 🔸 Type 1 — Router LSA

- كل راوتر يولد Router LSA.
    
- يحتوي على: معرف الراوتر (Router ID)، الشبكات المتصلة، حالة الواجهات، والتكلفة لكل واجهة.
    
- لا يغادر الـ Area أبدًا.
    

📍 مثال:  
R1 داخل Area 0 لديه واجهات إلى 10.0.0.0/24 و 10.0.1.0/24 → يعلن عنها في Type 1.

---

### 🔸 Type 2 — Network LSA

- يُنشأ فقط على الشبكات **Broadcast** أو **NBMA** التي تحتوي أكثر من راوتر.
    
- الـ **DR (Designated Router)** هو من يولدها.
    
- تصف كل الراوترات المتصلة بتلك الشبكة المشتركة.
    

📍 مثال:  
إذا كان 3 راوترات متصلة بسويتش في نفس الشبكة، الـ DR يصدر Type 2 ليقول "هنا شبكة مشتركة بين R1, R2, R3".

---

### 🔸 Type 3 — Summary LSA

- يُنشئها الـ **ABR** (الذي يربط منطقتين).
    
- تُستخدم لنقل معلومات عن شبكات Area إلى Area أخرى.
    
- تلخيص (Summary) لعدد كبير من الشبكات لتقليل حجم LSDB.
    

📍 مثال:  
Area 1 تحتوي على 10 شبكات داخلية → الـ ABR يصدر Type 3 واحدة إلى Area 0 تلخّصها مثلاً في 10.0.0.0/16.

---

### 🔸 Type 4 — ASBR Summary LSA

- تُستخدم عندما يوجد راوتر **ASBR** داخل Area ما.
    
- الـ ABR يصدر Type 4 ليُخبر باقي الـ Areas كيف تصل إلى ذلك الـ ASBR.
    

📍 مثال:  
إذا كان ASBR في Area 1، فالـ ABR يُرسل Type 4 إلى Area 0 ليُعلن مكانه.

---

### 🔸 Type 5 — External LSA

- يُنشئها الـ **ASBR** ليعلن عن شبكات من خارج OSPF (مثل من BGP أو Static).
    
- تُوزّع إلى جميع المناطق (باستثناء الـ Stub و Totally Stubby).
    
- يمكن أن تكون:
    
    - **E1**: التكلفة الخارجية + تكلفة OSPF الداخلية
        
    - **E2**: تكلفة خارجية فقط (الافتراضي)
        

📍 مثال:  
ASBR عنده static route إلى 8.8.8.8 → يعلن عنها في Type 5 لتُعرف داخل OSPF.

---

## 🔒 أنواع إضافية (في بيئات خاصة)

|النوع|الاسم|الوظيفة|
|---|---|---|
|**Type 6**|Group Membership LSA|خاص بـ Multicast OSPF (MOSPF)|
|**Type 7**|NSSA External LSA|بديل Type 5 داخل مناطق NSSA|
|**Type 8–11**|OSPFv3 (IPv6) أنواع إضافية|تخص IPv6 prefixes وlinks|

📍 **Type 7** مهم لأنه يُستخدم في المناطق **NSSA** (Not-So-Stubby Area) ليحمل معلومات خارجية تتحول لاحقًا إلى Type 5 عند الـ ABR.

---

## 🔍 أوامر مفيدة لعرض الـ LSAs

### عرض قاعدة بيانات الـ LSAs:

```cisco
show ip ospf database
```

### عرض نوع معين:

```cisco
show ip ospf database router       ← Type 1
show ip ospf database network      ← Type 2
show ip ospf database summary      ← Type 3
show ip ospf database external     ← Type 5
```

---

## ⚙️ ملخص الفكرة:

|النوع|يولّده|المدى|الوظيفة|
|---|---|---|---|
|1|Router|داخل الـ Area|روابط الراوتر|
|2|DR|داخل الـ Area|الشبكات المشتركة|
|3|ABR|بين المناطق|تلخيص الشبكات|
|4|ABR|بين المناطق|إعلان موقع ASBR|
|5|ASBR|كل المناطق|الشبكات الخارجية|
|7|NSSA ASBR|NSSA|مثل Type 5 داخل NSSA|

---

## 💡 ملاحظة مهمة جداً:

كل **Area في OSPF تملك LSDB خاص بها**،  
أي أن LSAs Type 1 و2 لا تتبادل بين المناطق — فقط الـ ABR هو من يلخصها عبر Type 3 و4 و5.

---

هل ترغب أشرح لك الآن **كيف تتفاعل أنواع الـ LSAs مع المناطق (Area Types)**؟  
يعني مثلًا: أي LSAs تُمنع في Stub وNSSA وTotally Stubby Area — هذا الجزء ممتاز لفهم OSPF المتقدم 🔥