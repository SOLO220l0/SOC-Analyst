

---

# 🧱 **البوابة الاحتياطية (Redundant Gateway)**

---

## 📘 أولاً: ما هي البوابة الافتراضية (Gateway)؟

البوابة الافتراضية هي **عنوان IP** يستخدمه أي جهاز في الشبكة  
للوصول إلى **شبكات أخرى خارج شبكته المحلية (LAN)** —  
سواء كانت VLAN أخرى أو الإنترنت.

مثلاً 👇  
لو عندك شبكة:

> 192.168.10.0/24  
> فالبوابة تكون مثلاً:  
> 192.168.10.1

---

## ⚠️ ثانياً: المشكلة التي تحلها "البوابة الاحتياطية"

في التصميم العادي، كل الأجهزة تعتمد على **بوابة واحدة فقط** (Router أو Switch).  
إذا تعطل هذا الجهاز أو انقطع الكابل،  
➡️ **كل الأجهزة داخل الشبكة ستفقد الاتصال الخارجي بالكامل**!

وهنا تأتي أهمية **Redundant Gateway**  
أي وجود **بوابة بديلة تعمل تلقائيًا عند فشل الرئيسية**.

---

## 🧩 ثالثاً: الهدف من تصميم البوابة الاحتياطية

الهدف هو ضمان:

1. ✅ **الاستمرارية (High Availability)**
    
2. ⚡ **تحويل فوري وسلس (Failover)** عند توقف إحدى البوابات.
    
3. 💡 **عدم الحاجة لتغيير إعدادات الأجهزة** في الشبكة.
    

---

## ⚙️ رابعاً: كيف يعمل مفهوم البوابة الاحتياطية؟

فكر فيها بهذه الطريقة 👇  
يوجد **جهازان (Router أو Layer 3 Switch)**  
كلاهما متصلان بنفس VLAN،  
لكن المستخدمين يرون فقط **بوابة افتراضية واحدة (IP واحد مشترك)**.

في الواقع:

- أحد الجهازين يكون **Active (فعّال)**
    
- الآخر يكون **Standby (احتياطي)**
    
- عند فشل الجهاز الفعّال → يتحول الاحتياطي تلقائيًا إلى Active  
    بدون أن يشعر المستخدم بأي انقطاع.
    

---

## 🧠 خامساً: البروتوكولات المستخدمة في Redundant Gateway

|البروتوكول|الاسم الكامل|الشركة|الوظيفة|
|---|---|---|---|
|**HSRP**|Hot Standby Router Protocol|Cisco|خاص بشركة Cisco|
|**VRRP**|Virtual Router Redundancy Protocol|قياسي (Open Standard)|مدعوم من معظم الشركات|
|**GLBP**|Gateway Load Balancing Protocol|Cisco|يدعم الموازنة بين البوابات|

---

### 🔹 1. **HSRP (Hot Standby Router Protocol)**

- من تطوير **Cisco**.
    
- يُنشئ **عنوان IP افتراضي مشترك** بين راوترين أو أكثر.
    
- أحدهما **Active** والآخر **Standby**.
    
- عند فشل الـ Active → يتحول Standby فورًا ليأخذ دوره.
    

📘 مثال:

```
Router1(config)# interface vlan 10
Router1(config-if)# ip address 192.168.10.2 255.255.255.0
Router1(config-if)# standby 1 ip 192.168.10.1
Router1(config-if)# standby 1 priority 110
Router1(config-if)# standby 1 preempt

Router2(config)# interface vlan 10
Router2(config-if)# ip address 192.168.10.3 255.255.255.0
Router2(config-if)# standby 1 ip 192.168.10.1
Router2(config-if)# standby 1 priority 100
Router2(config-if)# standby 1 preempt
```

👆 العنوان **192.168.10.1** هو البوابة الافتراضية  
والجهاز صاحب **الأولوية الأعلى (priority)** يكون الـ Active.

---

### 🔹 2. **VRRP (Virtual Router Redundancy Protocol)**

- معيار مفتوح (Open Standard).
    
- مشابه جدًا لـ HSRP في الفكرة،  
    لكن يُستخدم في بيئات مختلطة بين أجهزة من شركات مختلفة (مثل Cisco + HP + Juniper).
    

---

### 🔹 3. **GLBP (Gateway Load Balancing Protocol)**

- من تطوير **Cisco** أيضًا.
    
- يقدّم ميزة إضافية:  
    → **موازنة الأحمال (Load Balancing)** بين أكثر من بوابة.
    
- بدلاً من وجود راوتر فعّال واحد فقط،  
    **كل الأجهزة تشارك في التوجيه بالتناوب**، مما يحقق أداء أعلى.
    

---

## 💡 سادساً: الفكرة العامة بالرسوم التوضيحية (نصّية)

```
           +-------------------+
           |    Router 1 (Active)  |
           | IP: 192.168.10.2     |
           +-------------------+
                     |
                     | VLAN 10
                     |
           +-------------------+
           |  Virtual IP: 192.168.10.1  |
           | (Gateway used by clients)  |
           +-------------------+
                     |
                     |
           +-------------------+
           |   Router 2 (Standby) |
           | IP: 192.168.10.3     |
           +-------------------+
```

- المستخدمون يضبطون البوابة على **192.168.10.1** فقط.
    
- إذا تعطل Router 1 → Router 2 يأخذ الدور تلقائيًا.
    

---

## 🧱 سابعاً: الفوائد الرئيسية

|الفائدة|الوصف|
|---|---|
|🟢 **الاستمرارية**|الشبكة لا تتوقف عند فشل جهاز واحد|
|⚡ **تحويل سريع**|الانتقال بين البوابات خلال ثوانٍ|
|🔁 **إدارة مرنة**|يمكن ترقية أو صيانة أحد الأجهزة دون انقطاع الخدمة|
|⚙️ **سهولة التكوين**|المستخدمون لا يحتاجون لتغيير أي إعدادات|

---

## 🚀 ثامناً: في بيئات VLANs وSVIs

في الشبكات التي تستخدم **Layer 3 Switches** و **SVIs**،  
يمكن تفعيل بروتوكولات مثل HSRP بين سويتشين لتوفير بوابة احتياطية لكل VLAN:

```
SW1(config)# interface vlan 10
SW1(config-if)# standby 10 ip 192.168.10.1

SW2(config)# interface vlan 10
SW2(config-if)# standby 10 ip 192.168.10.1
```

النتيجة:  
حتى لو تعطل أحد السويتشات، تظل VLAN تعمل دون توقف. 🔄

---

## 🧠 تاسعاً: ملخص سريع

|العنصر|البوابة الأساسية|البوابة الاحتياطية|البروتوكول|
|---|---|---|---|
|VLAN 10|SW1 – 192.168.10.2|SW2 – 192.168.10.3|HSRP|
|VLAN 20|SW1 – 192.168.20.2|SW2 – 192.168.20.3|VRRP|
|VLAN 30|SW1 – 192.168.30.2|SW2 – 192.168.30.3|GLBP|

---

هل ترغب أن أرسم لك **مخطط مرئي بالعربي** يوضح  
كيف تعمل الـ Redundant Gateway بين سويتشين باستخدام HSRP؟  
سيكون ممتاز للفهم البصري.

[[66- Core Routers]]
