 
الآن نجي لشرح مفصل لمفهوم **OSPF Cost** — أحد أهم عناصر عمل بروتوكول التوجيه **OSPF (Open Shortest Path First)**.

---

## 🧠 ما هو الـ OSPF Cost؟

الـ **Cost** في OSPF هو **الـMetric** أو القيمة التي يستخدمها البروتوكول ليحدد _“أفضل مسار”_ نحو الوجهة.

🔹 كل واجهة (Interface) في الراوتر تُعطى قيمة **Cost** تمثل كلفة المرور من خلالها.  
🔹 كلما كانت الـCost أقل → اعتُبر المسار **أفضل** وأسرع.  
🔹 المسار ذو أقل مجموع كلفة من المصدر إلى الوجهة هو الذي يدخل إلى **Routing Table**.

---

## ⚙️ المعادلة الأساسية لحساب الـCost

المعادلة الافتراضية في سيسكو:

[  
\text{Cost} = \frac{\text{Reference Bandwidth}}{\text{Interface Bandwidth}}  
]

🔸 **Reference Bandwidth** = 100 Mbps (افتراضياً في Cisco)  
🔸 **Interface Bandwidth** = سرعة الواجهة (مثلاً 10 Mbps أو 1000 Mbps)

---

## 📘 أمثلة عملية:

|سرعة الواجهة|الحساب|الـCost الناتج|
|---|---|---|
|10 Mbps|100 / 10|10|
|100 Mbps|100 / 100|1|
|1 Gbps|100 / 1000|0.1 → تُقرب إلى 1 (لأن القيم صحيحة فقط)|
|10 Gbps|100 / 10000|0.01 → تقرّب إلى 1|

🧩 النتيجة:  
كل واجهة أسرع من 100 Mbps سيظهر لها **Cost = 1**،  
لذلك من الأفضل تعديل قيمة **Reference Bandwidth** لتناسب الشبكات الحديثة.

---

## 🧰 تعديل Reference Bandwidth

إذا عندك شبكات بسرعات مختلفة (1G, 10G, 40G مثلاً)،  
من الأفضل تغيير القيمة الافتراضية لتفرق بينهم:

```
router ospf 1
 auto-cost reference-bandwidth 10000
```

👉 هذا يجعل الـ Reference = 10 Gbps.  
الآن ستصبح حسابات الـCost أكثر دقة.

---

## 🧩 مثال عملي بسيط

لدينا 3 راوترات:

```
R1 ---[100 Mbps]--- R2 ---[10 Mbps]--- R3
```

- Cost بين R1 وR2 = 1
    
- Cost بين R2 وR3 = 10  
    إذن المسار من R1 إلى R3 = **1 + 10 = 11**
    

إذا وجد طريق آخر من R1 إلى R3 بسرعة أفضل مثلاً (كل الوصلات 100 Mbps)،  
فـ OSPF يختار الطريق ذو **أقل مجموع Cost**.

---

## ⚙️ تعديل Cost يدويًا

يمكنك تعديل الـCost لأي واجهة يدويًا:

```
interface gig0/0
 ip ospf cost 50
```

🔹 هذا مفيد عندما تريد إجبار OSPF على تفضيل أو تجنّب مسار معين.

---

## 🔍 عرض الـCost في الراوتر

```
show ip ospf interface
```

يعرض السرعة والـCost لكل واجهة.

---

## 📊 ملخص سريع

|العنصر|الشرح|
|---|---|
|**OSPF Cost**|قيمة تحدد كلفة المسار|
|**كلما قلت الكلفة**|زادت أولوية المسار|
|**الحساب**|Reference Bandwidth ÷ Interface Bandwidth|
|**الافتراضي في سيسكو**|Reference = 100 Mbps|
|**يمكن تعديلها**|بـ `auto-cost reference-bandwidth`|
|**يمكن تحديدها يدويًا**|بـ `ip ospf cost`|

---

هل ترغب أن أشرح لك **كيف يستخدم OSPF الـCost في حساب أفضل طريق (Dijkstra Algorithm)** خطوة بخطوة مع رسم توضيحي؟