
# 🗺️ **OSPF Area Design**

(تصميم مناطق OSPF)

هذا الموضوع هو أساس فهم كيف تُقسم الشبكات الكبيرة باستخدام OSPF،  
وهو ضروري جدًا لمستوى **CCNA** وخصوصًا **CCNP** أيضًا.

خلينا نشرحه **خطوة بخطوة بطريقة مفهومة وبأمثلة عملية.** 👇

---

## 🎯 أولًا: ما هي الـ Area في OSPF؟

> الـ **Area** في OSPF هي تقسيم منطقي داخل الشبكة يهدف إلى **تقليل الحمل (load)** و**تسريع عملية التحديث**.

ببساطة:  
شبكتك الكبيرة ما تكون كلها “منطقة واحدة”، بل تقسمها إلى عدة مناطق **Areas** حتى:

- تقلل حجم قاعدة البيانات (LSDB)
    
- تقلل الـ LSAs اللي تنتشر داخل الشبكة
    
- تسهل إدارة التوجيه
    
- تسرّع عملية الـ SPF Calculation
    

---

## 🧩 المفهوم الأساسي

كل راوتر داخل OSPF ينتمي إلى **واحدة أو أكثر من المناطق (Areas)**  
لكن **كل المناطق يجب أن تتصل بمنطقة العمود الفقري (Backbone Area 0)** مباشرة أو بشكل منطقي.

```
       [ Area 1 ]
         /      \
R1 --- R2 --- R3 --- R4
         \
         [ Area 0 ]
          \
           [ Area 2 ]
```

---

## 🌐 أنواع الراوترات في تصميم OSPF

|نوع الراوتر|التعريف|وظيفته|
|---|---|---|
|**Internal Router**|جميع واجهاته داخل نفس المنطقة|يتبادل LSAs داخل الـ area فقط|
|**Backbone Router**|جزء من منطقة 0|يتعامل مع العمود الفقري|
|**Area Border Router (ABR)**|لديه واجهات في منطقتين مختلفتين|يربط المناطق ببعض|
|**Autonomous System Boundary Router (ASBR)**|يربط OSPF مع بروتوكول آخر (EIGRP, BGP...)|يحقن (injects) شبكات خارجية|

---

## 🔹 أنواع الـ Areas في OSPF

|النوع|الاسم|الخصائص|ما يُسمح فيه من LSAs|
|---|---|---|---|
|**Normal Area**|عادية|كل أنواع الـ LSAs مسموحة|1, 2, 3, 4, 5|
|**Stub Area**|منطقة محدودة|تمنع الـ External LSAs (Type 5)|1, 2, 3 فقط|
|**Totally Stubby Area**|أكثر تقييدًا|تمنع Type 3 و5 – فقط default route من ABR|1, 2 فقط (+ default)|
|**NSSA (Not-So-Stubby Area)**|تسمح بإضافة شبكات خارجية لكن لا تقبل Type 5 من الخارج|1, 2, 3, 7||

---

## 🧭 تصميم الـ Areas في الواقع

|الهدف|التصميم المقترح|
|---|---|
|شبكة صغيرة (أقل من 50 راوتر)|Area واحدة (Area 0 فقط)|
|شبكة متوسطة (50–200 راوتر)|Area 0 + Area 1 + Area 2 …|
|شبكة كبيرة جدًا (مؤسسة أو مزود خدمة)|تقسيم منطقي: كل فرع له Area، كلها تتصل بـ Area 0|

---

## 🧠 قاعدة ذهبية

> كل منطقة يجب أن تتصل بـ **Area 0** بشكل مباشر أو منطقي (Virtual Link).  
> إذا لم تتصل بـ Area 0 → OSPF لن يمرر التحديثات بينها.

---

## 🧩 مثال عملي للتصميم

```
     +-----------+
     |   Area 1  |
     |  10.1.x.x  |
     +-----+-----+
           |
           | ABR (R2)
           |
     +-----+-----+
     |   Area 0  |
     | Backbone  |
     +-----+-----+
           |
           | ABR (R3)
           |
     +-----------+
     |   Area 2  |
     |  10.2.x.x  |
     +-----------+
```

### الشرح:

- **R2** و **R3** هما **ABRs** لأنهما يربطان مناطق مختلفة.
    
- **Area 1 و Area 2** لا تتواصل مباشرة؛ يتم التوجيه عبر **Area 0** فقط.
    
- إذا أرادت **Area 1** الوصول إلى شبكة في **Area 2** → يمر الترافيك عبر الـ **Backbone**.
    

---

## 🧮 مثال إعداد عملي (Cisco IOS)

### 🔸 R1 – داخل Area 1

```cisco
router ospf 1
 router-id 1.1.1.1
 network 10.1.0.0 0.0.0.255 area 1
```

### 🔸 R2 – يربط Area 1 و 0 (ABR)

```cisco
router ospf 1
 router-id 2.2.2.2
 network 10.1.0.0 0.0.0.255 area 1
 network 10.0.0.0 0.0.0.3 area 0
```

### 🔸 R3 – يربط Area 0 و 2 (ABR)

```cisco
router ospf 1
 router-id 3.3.3.3
 network 10.0.0.4 0.0.0.3 area 0
 network 10.2.0.0 0.0.0.255 area 2
```

### 🔸 R4 – داخل Area 2

```cisco
router ospf 1
 router-id 4.4.4.4
 network 10.2.0.0 0.0.0.255 area 2
```

---

## 🧩 متى نستخدم الأنواع الخاصة من المناطق؟

|الحالة|الحل الأفضل|
|---|---|
|تريد تقليل الـ LSDB في منطقة فرعية|استخدم **Stub Area**|
|تريد فقط Default Route بدون تفاصيل|استخدم **Totally Stubby**|
|لديك فرع فيه اتصال لخارج الشبكة (Static/BGP) وتبي تمنع Type 5 من الدخول|استخدم **NSSA**|

---

## 🧾 أوامر مفيدة للمناطق

```cisco
show ip ospf
```

يعرض قائمة المناطق في الراوتر:

```
Routing Process "ospf 1" with ID 1.1.1.1
Supports only single TOS(TOS0) routes
It is an area border router
Number of areas in this router is 2. 2 normal 0 stub 0 nssa
```

```cisco
show ip ospf database
```

تشوف أنواع الـ LSAs حسب المناطق.

---

## ⚙️ نصائح في التصميم

✅ اجعل **Area 0** في مركز الشبكة (Core).  
✅ لا تضع أكثر من 50 راوتر داخل Area واحدة في الشبكات الكبيرة.  
✅ استخدم **Summarization** عند الـ ABRs لتقليل التحديثات.  
✅ استخدم **Passive Interfaces** للشبكات غير المتصلة براوترات أخرى.  
✅ استخدم **Stub Areas** للفروع الصغيرة التي لا تحتاج تفاصيل الشبكة الكاملة.

---

## 🧭 ملخص سريع:

|العنصر|الشرح|
|---|---|
|Area|تقسيم منطقي داخل OSPF|
|Area 0|العمود الفقري Backbone|
|ABR|يربط منطقتين|
|ASBR|يربط OSPF ببروتوكول آخر|
|Stub|تمنع Type 5|
|Totally Stubby|تمنع Type 3 و5|
|NSSA|تمنع Type 5 لكن تسمح بـ 7 داخليًا|

---

هل ترغب أشرح الآن **أنواع الـ Area بالتفصيل مع أوامر إعداد Stub / NSSA عمليًا على Cisco؟**  
(يعتبر الجزء التطبيقي لتصميم المناطق)