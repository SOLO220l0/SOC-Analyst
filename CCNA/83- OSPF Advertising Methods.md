ممتاز جدًا 🔥  
سؤالك “**OSPF Advertising Methods**” هو واحد من أهم المواضيع لفهم **كيف يعلن بروتوكول OSPF عن الشبكات** داخل النظام، سواء كانت **شبكات داخلية** (Internal) أو **خارجية** (External).



---

# 📢 **OSPF Advertising Methods**

(طرق الإعلان داخل OSPF)

---

## 🎯 الفكرة الأساسية

> في بروتوكول OSPF، عملية **Advertising** تعني:  
> كيف يعلن الراوتر عن الشبكات (الـ IP Networks) التي يعرفها لبقية الراوترات داخل الـ OSPF domain.

أي شبكة تريد نشرها عبر OSPF يجب أن تُعلن باستخدام **واحدة من طرق الإعلان (Advertising Methods)**.

---

## 🧩 أنواع الإعلان في OSPF

بشكل عام، توجد **ثلاث طرق أساسية** لإعلان الشبكات داخل OSPF:

|النوع|الاسم|متى يُستخدم|نوع الـ LSA الناتج|
|---|---|---|---|
|1️⃣ **Direct Network Advertisement**|الإعلان المباشر عن الشبكات المتصلة|لتفعيل OSPF على واجهات محددة|Type 1, 2 LSAs|
|2️⃣ **Summary Advertisement**|تلخيص الشبكات بين المناطق (Area to Area)|عند الـ ABR|Type 3, 4 LSAs|
|3️⃣ **External Advertisement**|إعلان شبكات من بروتوكولات أو مصادر خارج OSPF|عند الـ ASBR|Type 5 أو Type 7 LSAs|

---

## 🔹 1. Direct Network Advertisement

(الإعلان المباشر عن الشبكات المتصلة)

هذه هي الطريقة الأساسية والأكثر استخدامًا في OSPF.

تعلن الشبكات **المتصلة مباشرة بالراوتر** عن طريق الأمر:

```cisco
router ospf 1
 network <network-address> <wildcard-mask> area <area-id>
```

### مثال:

```cisco
router ospf 1
 network 10.1.1.0 0.0.0.255 area 0
```

🎯 ما يحدث:

- الراوتر يفعّل OSPF على الواجهة التي تقع ضمن الشبكة المعلنة.
    
- يبدأ إرسال **Hello packets**.
    
- يُعلن عن الشبكة ضمن LSAs داخل الـ Area.
    

💡 ينتج عن ذلك:

- **Type 1 LSA** (Router LSA): يحتوي على معلومات عن واجهات الراوتر.
    
- **Type 2 LSA** (Network LSA): يُستخدم في الشبكات متعددة الوصول (مثل Ethernet).
    

---

## 🔹 2. Summary Advertisement

(تلخيص الشبكات بين المناطق – Area Summarization)

عندما يربط **راوتر (ABR)** أكثر من منطقة، يقوم بإعلان شبكات من منطقة إلى أخرى **بشكل مُلخص (Summary)**.

### الغرض:

- تقليل حجم جدول التوجيه.
    
- تقليل حجم LSDB (Link State Database).
    
- تحسين الأداء.
    

### مثال:

لنفترض أن **Area 1** تحتوي على هذه الشبكات:

```
10.1.1.0/24  
10.1.2.0/24  
10.1.3.0/24
```

يمكننا تلخيصها في شبكة واحدة:

```cisco
router ospf 1
 area 1 range 10.1.0.0 255.255.0.0
```

🎯 النتيجة:

- الراوتر (ABR) يُعلن شبكة واحدة (10.1.0.0/16) بدل 3 شبكات.
    
- ينتج **Type 3 LSA** لإرسال الشبكات بين المناطق.
    

---

### 🔸 أيضًا داخل ABR:

لو كان عندك ASBR داخل منطقة فرعية، الـ ABR ممكن يولّد **Type 4 LSA** لتحديد موقع الـ ASBR في المناطق الأخرى.

---

## 🔹 3. External Advertisement

(الإعلان عن الشبكات الخارجية – External Routes)

عندما يكون لديك راوتر يربط OSPF ببروتوكول آخر (مثل Static أو BGP)،  
هذا الراوتر يُسمى **ASBR (Autonomous System Boundary Router)**.

وهو المسؤول عن **إعادة توزيع (Redistribution)** الشبكات الخارجية داخل OSPF.

### المثال:

```cisco
ip route 192.168.50.0 255.255.255.0 10.1.1.2
!
router ospf 1
 redistribute static subnets
```

🎯 النتيجة:

- يتم حقن الشبكة (192.168.50.0/24) داخل OSPF.
    
- تُعلن هذه الشبكة في LSAs من نوع **Type 5 (External LSA)**.
    

---

### أنواع الشبكات الخارجية:

|النوع|الاسم|طريقة الحساب|
|---|---|---|
|**E1**|External Type 1|يضيف تكلفة OSPF الداخلية + الخارجية|
|**E2**|External Type 2|يستخدم فقط التكلفة الخارجية (الافتراضي)|

```cisco
redistribute static metric-type 1 subnets
```

يُستخدم لتحديد أن الشبكة من نوع E1.

---

### 🔹 الإعلان الخارجي في مناطق NSSA

بعض المناطق (مثل NSSA) لا تسمح بـ Type 5 LSAs،  
فتُستخدم **Type 7 LSAs** بدلها.

ثم يقوم الـ ABR بتحويلها إلى **Type 5** عندما تصل إلى الـ Backbone.

---

## ⚙️ ملخص لأنواع الإعلانات داخل OSPF

|الطريقة|المكان الذي تُستخدم فيه|نوع الـ LSA الناتج|الغرض|
|---|---|---|---|
|**Direct**|داخل نفس الـ Area|Type 1 & 2|إعلان الشبكات المحلية|
|**Summary**|بين المناطق (ABR)|Type 3 & 4|تلخيص الشبكات|
|**External**|عند الـ ASBR|Type 5 أو 7|إعلان الشبكات الخارجية|

---

## 🧠 مثال شامل للتوضيح:

```
         [ Area 1 ]
       10.1.1.0/24
            |
        (ABR)
            |
       [ Area 0 ]
            |
        (ASBR)
            |
     Static Route 192.168.50.0/24
```

🔸 داخل Area 1 → Direct Advertisement (Type 1,2)  
🔸 من Area 1 إلى Area 0 → Summary Advertisement (Type 3)  
🔸 من ASBR إلى باقي الشبكة → External Advertisement (Type 5)

---

## 🧩 أوامر تحقق مهمة:

```cisco
show ip ospf database
```

تشاهد جميع أنواع الـ LSAs.

```cisco
show ip route ospf
```

تشوف نوع المسار:

- `O` → داخلي (Intra-area)
    
- `O IA` → بين المناطق (Inter-area)
    
- `O E1/E2` → خارجي (External)
    

---

## 📘 ملخص سريع:

|التصنيف|الوسيلة|LSA Type|الوظيفة|
|---|---|---|---|
|Direct Advertisement|`network` أو `ip ospf area`|1, 2|إعلان الشبكات الداخلية|
|Summary Advertisement|`area <id> range`|3, 4|تلخيص الشبكات بين المناطق|
|External Advertisement|`redistribute ...`|5, 7|إعلان الشبكات الخارجية|

---

هل ترغب أشرح الآن **أنواع الـ LSAs بالتفصيل (1 إلى 7)** وكيف يعلن كل نوع الشبكات عمليًا في Cisco؟  
بيكون تكملة ممتازة بعد "Advertising Methods" 🔥