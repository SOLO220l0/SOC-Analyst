

---

## 🧠 المفهوم العام

مصطلح **OSPF Hidden Router** لا يعني نوع جديد من الراوترات،  
بل هو **حالة أو سلوك** يحدث داخل شبكة OSPF عندما لا يتمكن راوتر معيّن من الظهور في جدول الجيران (Neighbor Table) أو جدول التوجيه (Routing Table) بالرغم من أنه **جزء من الشبكة فعلياً**.

بمعنى آخر 👇  
الراوتر يكون موجود ومتصل فعلاً، لكن الـOSPF لا يراه ولا يتبادل معه الـLSAs،  
فيُعتبر “مخفي” من وجهة نظر الـOSPF.

---

## ⚙️ الأسباب المحتملة لظهور "Hidden Router"

1. ### 🔸 مشكلة في الـOSPF Network Type
    
    - لو كانت الواجهة مثلاً **Point-to-Point** في راوتر و**Broadcast** في الآخر،  
        فلن يتبادلا التحية (Hello packets) ولن يصبحا جيران.
        
    - النتيجة: كل واحد يعتقد أنه وحيد في الشبكة → الآخر “مخفي”.
        
2. ### 🔸 اختلاف قيم الـHello / Dead Interval
    
    - هذان المؤقتان لازم يتطابقان بين الراوترين.
        
    - لو واحد يرسل Hello كل 10 ثواني والثاني ينتظر كل 5 → لن تتكوّن علاقة Neighbor.
        
3. ### 🔸 Passive Interface
    
    - إذا تم تفعيل `passive-interface` على واجهة معيّنة،  
        OSPF **لن يرسل Hello** من تلك الواجهة.
        
    - النتيجة: الراوتر المقابل لن يراه → "Hidden".
        
4. ### 🔸 مشكلة في العناوين أو الشبكات المعلنة
    
    - إذا لم تُعلن الشبكة في عملية OSPF (بأمر `network` المناسب)،  
        فلن تعتبر تلك الواجهة جزء من OSPF.
        
5. ### 🔸 فلترة LSAs أو استخدام Distribute-list / Area filter
    
    - يمكن أن تكون الراوترات في نفس المنطقة، لكن الـLSA الخاصة بأحدها لا يتم تمريرها.  
        فيظهر “مخفي” في جدول التوجيه.
        
6. ### 🔸 Authentication mismatch
    
    - لو كان في Authentication مفعل على OSPF (MD5 مثلاً)  
        والمفاتيح غير متطابقة، فلن يتكوّن الجوار.
        

---

## 🧩 النتيجة

الـ **Hidden Router** يعني:

- لا يوجد تبادل LSAs مع الجيران.
    
- الراوتر غير موجود في الـLSDB (Link-State Database).
    
- شبكاته غير موجودة في الـRouting Table لدى الآخرين.
    

---

## 🔍 الحلول أو طرق الفحص

1. استخدام الأمر:
    
    ```
    show ip ospf neighbor
    ```
    
    → للتحقق هل الجار موجود أم لا.
    
2. التحقق من نوع الشبكة:
    
    ```
    show ip ospf interface
    ```
    
3. التأكد من الأوامر:
    
    ```
    router ospf 1
    network <ip> <wildcard> area <id>
    ```
    
4. فحص الـHello/Dead intervals:
    
    ```
    show ip ospf interface <int>
    ```
    
5. فحص الـAuthentication إن وجد:
    
    ```
    show ip ospf interface <int> | include Auth
    ```
    

---

## 🧭 مثال عملي

راوترين متصلين عبر FastEthernet0/0:

### Router A:

```
router ospf 1
 network 10.1.1.0 0.0.0.255 area 0
```

### Router B:

```
router ospf 1
 network 10.1.1.0 0.0.0.255 area 0
```

إذا فعلت على Router A:

```
passive-interface fa0/0
```

فـ Router B لن يتلقى Hello packets،  
وبالتالي Router A يصبح **Hidden Router** بالنسبة لـ Router B.

---

هل ترغب أن أعمل لك **رسم توضيحي (Diagram)** يبين كيف يصير الراوتر "مخفي" في OSPF؟  
يساعدك كثير في فهم الفكرة بصرياً.