
---

## 🎯 ما هو الـ Metric في OSPF؟

الـ **Metric** في OSPF يسمى **Cost (التكلفة)**، وهو الرقم الذي يستخدمه الراوتر ليقرر **أي مسار هو الأفضل داخل OSPF** عندما توجد **عدة مسارات لنفس الوجهة**.

> القاعدة: كلما كانت **التكلفة (Cost)** أصغر → كان المسار **أفضل**.

---

## 🧮 كيف تُحسب تكلفة OSPF؟

الصيغة الأساسية:

```
Cost = Reference Bandwidth / Interface Bandwidth
```

- **Reference Bandwidth**: القيمة المرجعية الافتراضية في Cisco = 100 Mbps
    
- **Interface Bandwidth**: سرعة الواجهة (بالـ Mbps)
    

---

## 🔹 مثال 1:

واجهة بسرعة **FastEthernet (100 Mbps)**

```
Cost = 100 / 100 = 1
```

واجهة بسرعة **GigabitEthernet (1000 Mbps)**

```
Cost = 100 / 1000 = 0.1 → يقرب إلى 1
```

إذن كل من 100 و1000 Mbps ستكون تكلفتها "1"، لذلك يفضل تعديل الـ reference.

---

## ⚙️ تغيير الـ Reference Bandwidth

لأن الشبكات الحديثة أسرع بكثير (10G, 40G, 100G)، من الأفضل تعديل الـ reference حتى تكون الحسابات منطقية:

```cisco
router ospf 1
 auto-cost reference-bandwidth 10000
```

هنا جعلنا المرجع = **10000 Mbps (10 Gbps)**  
الآن:

- FastEthernet (100 Mbps): cost = 10000 / 100 = **100**
    
- GigabitEthernet (1000 Mbps): cost = 10000 / 1000 = **10**
    
- 10 Gigabit: cost = 10000 / 10000 = **1**
    

🔹 هذه الطريقة تجعل الراوتر يفضّل فعلاً المسار الأسرع.

---

## 💡 ملاحظة مهمة:

- OSPF ينظر إلى **مجموع التكاليف** من الراوتر الحالي حتى الوجهة.
    
- كل راوتر يحسب تكلفته الخاصة **لكل رابط** ويجمعها حتى الوجهة النهائية.
    
- أقل مجموع تكلفة = أفضل مسار.
    

---

## 🔧 تعديل الـ Cost يدويًا على واجهة معينة:

```cisco
interface GigabitEthernet0/0
 ip ospf cost 50
```

> يُستخدم هذا غالبًا للتحكم اليدوي في اختيار المسار (مثلاً لعمل Load Balancing أو Path Preference).

---

## 🧠 مثال توضيحي عملي:

لديك 3 راوترات:

```
R1 ---100Mbps--- R2 ---1Gbps--- R3
```

### المسار 1 (R1 → R2 → R3):

- R1–R2: cost = 100/100 = 1
    
- R2–R3: cost = 100/1000 = 0.1 (يقرب إلى 1)  
    🔹 المجموع = 2
    

إذا وُجد مسار آخر:

```
R1 ---10Mbps--- R3
```

cost = 100/10 = 10  
🔹 OSPF يختار المسار عبر R2 لأن المجموع = 2 فقط.

---

## 🧩 أنواع الـ Metrics في OSPF

|البند|المعنى|
|---|---|
|**Cost**|المقياس الأساسي|
|**Cumulative Cost**|مجموع تكاليف الروابط حتى الوجهة|
|**Equal Cost Multi Path (ECMP)**|إذا كانت التكلفة متساوية لمسارين → يستخدم OSPF كلاهما (Load Balancing)|

> في Cisco يمكن لراوتر OSPF استخدام **حتى 4 مسارات متساوية** بشكل افتراضي.

---

## 📋 عرض الـ Cost والمسارات:

```cisco
show ip ospf interface
```

يُظهر التكلفة الحالية لكل واجهة.

```cisco
show ip route ospf
```

يعرض المسارات OSPF مع تكلفة كل مسار في الجدول.

---

## ⚠️ نصيحة مهمة:

إذا عدلت الـ reference bandwidth على راوتر، لازم تعدله على **كل الراوترات** في الشبكة OSPF حتى لا تختلف الحسابات وتسبب اختيارات غير متوقعة.

---

## 🧩 الخلاصة السريعة:

|المصطلح|القيمة الافتراضية / الملاحظة|
|---|---|
|Reference Bandwidth|100 Mbps|
|Formula|Cost = Ref BW / Interface BW|
|أقل Cost|أفضل مسار|
|تعديل cost|`ip ospf cost X`|
|تعديل ref|`auto-cost reference-bandwidth X`|
|Equal Cost Paths|يدعم حتى 4 بشكل افتراضي|

---

هل ترغب أكمل لك الآن بالشرح التالي 👇  
**"OSPF Path Selection Process"** — كيف يقرر OSPF فعليًا أي Route يدخل في الـ Routing Table خطوة بخطوة؟