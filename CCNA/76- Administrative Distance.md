
---

## 🎯 ما هو Administrative Distance (المسافة الإدارية)؟

هي **رقم يستخدمه الراوتر لتحديد مدى موثوقية مصدر المسار (Route Source)** عندما يتلقى **عدة مسارات لنفس الشبكة** من بروتوكولات مختلفة.

> ببساطة: **كلما كان الرقم أصغر → المسار أكثر ثقة.**

---

## 🧠 مثال لفهم الفكرة:

تخيل عندك راوتر يتعلم الشبكة `10.0.0.0/24` من أكثر من مصدر:

- من **Static route**
    
- ومن **OSPF**
    
- ومن **RIP**
    

الراوتر لازم يختار **واحد فقط** يضيفه إلى **Routing Table**، صح؟  
هنا يجي دور **Administrative Distance (AD)** 👇

|مصدر المسار|AD الافتراضي|التفسير|
|---|---|---|
|Connected Interface|0|الأعلى ثقة — الواجهة متصلة مباشرة|
|Static Route|1|ثاني أعلى ثقة|
|EIGRP (internal)|90|موثوق جداً|
|OSPF|110|موثوق متوسط|
|RIP|120|ضعيف الثقة|
|External EIGRP|170|أضعف|
|Unknown/Invalid|255|لا يُستخدم إطلاقاً|

🔹 فالراوتر في المثال سيختار المسار القادم من **Static Route** لأن AD = 1 (أقل رقم).

---

## ⚙️ كيف يعمل الـ AD داخل الراوتر

عندما يتعلم الراوتر مسارات من بروتوكولات مختلفة، يقارن الـ AD فقط إذا كانت **وجهتها (destination network) واحدة**.  
مثلاً:

- لو شبكة `192.168.1.0/24` تأتي من OSPF وRIP، الراوتر يختار **OSPF** لأن AD=110 < 120.
    
- لكنه قد يحتفظ بالـ RIP كمصدر احتياطي (floating route) إذا تعطل الـ OSPF.
    

---

## 💡 ملاحظة مهمة

> الـ AD يُستخدم فقط بين بروتوكولات **مختلفة**، وليس داخل البروتوكول نفسه.  
> داخل البروتوكول (مثل OSPF) يتم الاختيار حسب **المسافة (metric/cost)** وليس AD.

---

## 🔧 أوامر في Cisco

### عرض المسافات الإدارية الحالية:

```cisco
show ip route
```

يعرض في الجدول كل مسار ومصدره، وسترى بجانبه رقم AD.

### لتغيير المسافة الإدارية لمسار ثابت:

```cisco
ip route 10.0.0.0 255.255.255.0 192.168.1.1 5
```

الرقم "5" هنا هو الـ **Administrative Distance** — يعني هذا المسار الثابت لن يُستخدم إلا إذا لم يوجد مسار AD أقل.

> يُسمى هذا أحياناً **Floating Static Route** (مسار ثابت احتياطي).

---

## 🔁 مقارنة داخل نفس البروتوكول

لو كان المصدر **نفس البروتوكول** (مثلاً OSPF فقط)، فإن الراوتر يستخدم **المتريك (metric)** وليس الـ AD:

- في OSPF → يستخدم الـ **Cost**.
    
- في EIGRP → يستخدم الـ **Composite Metric**.
    
- في RIP → يعتمد على **Hop Count**.
    

---

## 🔍 مثال عملي

لنفترض أن الراوتر يتعلم الشبكة `172.16.0.0/16` من:

- Static route عبر `10.0.0.2` (AD=1)
    
- OSPF route عبر `10.0.0.3` (AD=110)
    

النتيجة:

```
S    172.16.0.0/16 [1/0] via 10.0.0.2
```

الـ static route يضاف إلى جدول التوجيه لأن AD=1 (الأقل).

---

## 🧩 الخلاصة السريعة:

|نوع المصدر|AD الافتراضي|ملاحظات|
|---|---|---|
|Directly Connected|0|واجهة محلية|
|Static|1|قوي جداً|
|EIGRP (internal)|90|موثوق|
|OSPF|110|متوسط|
|RIP|120|ضعيف|
|EIGRP (external)|170|خارجي|
|Unknown|255|مرفوض|

---

هل ترغب أشرح لك الآن **الفرق بين Administrative Distance و Metric**؟  
كثير من الطلاب يخلطون بينهم، فممكن أشرحهم مع مثال عملي 👇