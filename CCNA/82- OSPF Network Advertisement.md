
# 📢 **OSPF Network Advertisement**

(الإعلان عن الشبكات داخل OSPF)

---

## 🎯 الفكرة الأساسية

عندما تفعّل بروتوكول **OSPF** على الراوتر، تحتاج أن **تخبره بأي شبكات تريد الإعلان عنها (Advertise)** حتى تتم إضافتها إلى **جدول التوجيه (Routing Table)** ويتم نشرها إلى بقية الراوترات.

بمعنى آخر:

> “OSPF Network Advertisement” هي العملية التي يعلن فيها الراوتر عن **الشبكات المتصلة مباشرة به** لبقية الراوترات في نفس الـ Area.

---

## 🧩 كيف يتم الإعلان داخل OSPF؟

هناك طريقتان رئيسيتان لإعلان الشبكات في OSPF:

### 1️⃣ الطريقة التقليدية (باستخدام أمر `network`)

في إعداد OSPF نستخدم الأمر:

```cisco
network <network-address> <wildcard-mask> area <area-number>
```

### وظيفة هذا الأمر:

- يحدد **أي واجهات (Interfaces)** سيتم تفعيل OSPF عليها.
    
- يعلن الشبكة المتصلة بالواجهة ضمن **الـ LSAs**.
    
- يحدد **أي Area** تنتمي إليها الواجهة.
    

---

## 🧮 مثال عملي

نفترض عندك راوتر فيه الواجهات التالية:

|Interface|IP Address|Network|
|---|---|---|
|Gig0/0|10.1.1.1/24|LAN Users|
|Gig0/1|192.168.1.1/30|Link to R2|
|Loopback0|1.1.1.1/32|Router ID|

نريد تفعيل OSPF على الشبكات السابقة.

### الإعداد:

```cisco
router ospf 1
 router-id 1.1.1.1
 network 10.1.1.0 0.0.0.255 area 0
 network 192.168.1.0 0.0.0.3 area 0
```

🎯 النتيجة:

- الراوتر يفعل OSPF على الواجهات التي تقع ضمن هذه الشبكات.
    
- يرسل LSAs تحتوي على الشبكات المعلن عنها.
    
- كل الراوترات في نفس الـ area تتعلم هذه الشبكات وتضيفها لجدول التوجيه.
    

---

## 🧠 ملاحظة مهمة:

الـ **Wildcard mask** عكس الـ **Subnet mask**.

|Subnet mask|Wildcard mask|
|---|---|
|255.255.255.0|0.0.0.255|
|255.255.255.252|0.0.0.3|
|255.255.255.255|0.0.0.0|

---

## 🧩 الطريقة الثانية (باستخدام الواجهة مباشرة)

بدل استخدام `network`، يمكن تفعيل OSPF مباشرة على الواجهة:

```cisco
interface Gig0/0
 ip ospf 1 area 0
```

✅ الميزة:

- أدق وأوضح (تستخدمها كثير في CCNP).
    
- لا تحتاج إلى wildcard mask.
    

---

## 🔁 كيف يتم إرسال الشبكات داخل OSPF؟

بعد تفعيل الـ OSPF على الواجهات:

1. الراوتر يرسل **Hello Packets** لاكتشاف الجيران (Neighbors).
    
2. يتم تبادل **Database Description (DBD)** و **Link State Updates (LSU)**.
    
3. تنشأ **LSAs (Link-State Advertisements)** التي تحتوي على تفاصيل الشبكات.
    
4. تنتشر LSAs إلى جميع الراوترات داخل الـ Area.
    
5. كل راوتر يبني **LSDB (Link-State Database)** ويحسب أفضل المسارات باستخدام **Dijkstra SPF Algorithm**.
    

---

## 🧱 أنواع الشبكات التي يمكن إعلانها

|نوع الشبكة|التوضيح|
|---|---|
|**Directly Connected Networks**|الشبكات المتصلة مباشرة بالراوتر|
|**Loopback Interfaces**|تُستخدم عادة كـ Router ID أو اختبارات|
|**Summarized Networks**|يمكن تلخيصها عند الـ ABR أو ASBR|
|**External Networks**|شبكات من بروتوكولات أخرى (يعلن عنها ASBR كـ Type 5 LSA)|

---

## ⚙️ أمثلة واقعية:

### 🔸 إعلان شبكة داخل Area 0

```cisco
router ospf 1
 network 10.10.10.0 0.0.0.255 area 0
```

### 🔸 إعلان شبكة داخل Area 1

```cisco
router ospf 1
 network 172.16.1.0 0.0.0.255 area 1
```

### 🔸 إعلان شبكة Loopback

```cisco
interface Loopback0
 ip address 1.1.1.1 255.255.255.255
!
router ospf 1
 network 1.1.1.1 0.0.0.0 area 0
```

---

## 🧭 إعلان شبكات خارجية (External Networks)

إذا كنت تريد الإعلان عن شبكة من **بروتوكول آخر أو Static Route** داخل OSPF،  
فهذا يتم من خلال **Redistribution** (عبر ASBR):

```cisco
router ospf 1
 redistribute static subnets
```

وهذا يولّد **Type 5 LSA** داخل الشبكة، وتُسمى:

> **External OSPF Route (E1 أو E2)**

---

## 🧩 أوامر التحقق

### عرض الشبكات المعلنة:

```cisco
show ip ospf interface
```

### عرض الشبكات في جدول التوجيه:

```cisco
show ip route ospf
```

### عرض قاعدة بيانات الـ LSAs:

```cisco
show ip ospf database
```

---

## 💡 نصائح في الإعلانات (Advertisements)

✅ استخدم Loopback كـ Router ID دائمًا.  
✅ لا تعلن شبكات لا تحتاجها (قلّل الحمل).  
✅ استخدم Summarization عند ABR/ASBR لتقليل عدد الـ LSAs.  
✅ تأكد أن كل الشبكات داخل نفس الـ Area متصلة فعلاً.

---

## 📘 ملخص سريع:

|العنصر|الشرح|
|---|---|
|Network Advertisement|عملية إعلان الشبكات داخل OSPF|
|أمر الإعلان|`network <ip> <wildcard> area <area-id>`|
|البديل|`ip ospf 1 area <id>` داخل الواجهة|
|مسؤول الإعلان|الـ Router نفسه|
|النتيجة|إنشاء LSAs ونشر الشبكات|
|الشبكات الخارجية|يتم إعلانها بـ Redistribution|

---

هل ترغب أشرح لك الآن **كيف يتم إعلان الشبكات الخارجية (External Routes Type 5 & 7) بالتفصيل داخل OSPF** مع أمثلة عملية؟  
هذا هو الجزء التالي المنطقي بعد “Network Advertisement”.