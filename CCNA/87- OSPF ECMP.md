
موضوع **OSPF ECMP** (اختصار لـ _Equal-Cost Multi-Path_) من أهم وأجمل المفاهيم في بروتوكول OSPF،  
لأنه يتكلم عن **موازنة التحميل (Load Balancing)** بين مسارات متساوية الكلفة.


---

## 🧠 ما هو OSPF ECMP؟

**ECMP (Equal-Cost Multi-Path)** تعني أن:

> الراوتر يمكنه استخدام أكثر من مسار واحد نحو نفس الوجهة **إذا كانت هذه المسارات لها نفس الـCost**.

🔹 يعني لو عندك طريقين أو أكثر إلى نفس الشبكة،  
وكلهم بنفس الكلفة (OSPF Cost)،  
فـ OSPF يضيفهم جميعًا إلى **جدول التوجيه (Routing Table)** ويستخدمهم معًا بالتوازي.

---

## 🎯 الهدف من ECMP

الغرض الأساسي هو:

- ✅ **توزيع الحمل (Load Balancing)** على أكثر من رابط.
    
- ✅ **تحسين الأداء**.
    
- ✅ **توفير اعتمادية أعلى (Redundancy)** في حال انقطع أحد المسارات.
    

---

## ⚙️ كيف يعمل OSPF ECMP؟

OSPF يستخدم خوارزمية **Dijkstra (Shortest Path First)** لحساب أقل تكلفة (Cost) نحو كل شبكة.  
إذا وجد أكثر من مسار له نفس أقل تكلفة،  
فإنه **لا يستبعد الباقي**، بل يحتفظ بهم جميعًا كمسارات متساوية الكلفة.

ثم نظام التشغيل (مثل Cisco IOS) يرسل الحزم على هذه المسارات **بالتناوب أو حسب خوارزمية معينة**.

---

## 🧩 مثال توضيحي

```
         [R2]
        /     \
   10Mbps      10Mbps
      /           \
 [R1]-------------[R3]
         10Mbps
```

- من R1 إلى R3 يوجد مساران عبر R2 أو مباشرة.
    
- لو كانت كل الروابط **بنفس السرعة (10 Mbps)**  
    → الكلفة من R1 إلى R3 عبر أي طريق ستكون **متساوية**.
    

النتيجة:

```
R1 Routing Table:
Network 10.3.3.0/24 via R2, Cost 10
Network 10.3.3.0/24 via Direct Link, Cost 10
```

✅ OSPF يضع **المسارين الاثنين** في جدول التوجيه.  
✅ الراوتر يستخدمهما **في نفس الوقت**.

---

## ⚙️ عدد المسارات التي يدعمها OSPF

في أجهزة Cisco:

- **افتراضيًا:** OSPF يسمح بـ **4 مسارات متساوية الكلفة**.
    
- **يمكن زيادتها** إلى حد أقصى **16 مسار** بالأمر:
    

```
router ospf 1
 maximum-paths 8
```

---

## 🔄 أنواع الـ Load Balancing في ECMP

|النوع|الشرح|
|---|---|
|**Per-Packet**|كل حزمة (Packet) تُرسل عبر مسار مختلف بالتناوب. (يقلل من الكفاءة أحيانًا)|
|**Per-Destination**|كل وجهة (IP) يتم إرسال حزمها عبر نفس المسار لتفادي تغيير الترتيب. (الأكثر شيوعًا)|
|**Per-Flow (CEF)**|يستخدم خوارزمية (Cisco Express Forwarding) لاختيار المسار لكل جلسة بناءً على المصدر والوجهة.|

---

## 🧰 عرض مسارات ECMP في الراوتر

```
show ip route ospf
```

لو كان هناك أكثر من مسار متساوي، ستلاحظ أن:

```
O 10.10.10.0/24 [110/20] via 10.1.1.2, 00:00:12, Gig0/0
                              via 10.2.2.2, 00:00:12, Gig0/1
```

🟢 أي أن الشبكة 10.10.10.0 يمكن الوصول إليها عبر **مسارين متساويين**.

---

## 📊 الفوائد الرئيسية

✅ تحسين استخدام عرض النطاق (Bandwidth).  
✅ تقليل زمن الاستجابة.  
✅ زيادة التوافرية (Redundancy).  
✅ انتقال سلس عند فشل أحد المسارات.

---

## ⚠️ ملاحظات مهمة

- OSPF لا يقوم بـECMP إذا كانت الكلفة مختلفة حتى لو الفرق بسيط جدًا.
    
- يجب أن تكون المسارات نحو نفس الـDestination بنفس الـCost ونفس نوع الـArea.
    
- بعض الأجهزة قد تحتاج تفعيل خاصية CEF لضمان التوزيع المتوازن الصحيح.
    

---

هل ترغب أعمل لك **رسم توضيحي (Diagram)** يوضح كيف OSPF يوزع الترافيك في ECMP بين أكثر من مسار خطوة بخطوة؟  
يسهّل الفهم كثير بصريًا.