
خلينا الآن نشرح بالتفصيل مفهوم **OSPF Advertised Cost** — وهو من المفاهيم الدقيقة والمهمة في فهم كيفية تبادل المعلومات بين الراوترات في بروتوكول **OSPF (Open Shortest Path First)**.

---

## 🧠 ما هو الـ OSPF Advertised Cost؟

الـ **Advertised Cost** تعني:

> الكلفة (Cost) التي يُعلنها الراوتر لجيرانه في OSPF كقيمة للوصول إلى شبكة أو وجهة معينة.

بمعنى آخر 👇  
كل راوتر داخل OSPF عندما يرسل **LSA (Link-State Advertisement)**،  
فهو يذكر داخلها الكلفة (Cost) التي يراها هو من نفسه إلى تلك الشبكة أو إلى جيرانه.  
هذه القيمة تُسمى **Advertised Cost**.

---

## 📊 الفرق بين Cost و Advertised Cost

|المصطلح|المعنى|
|---|---|
|**Interface Cost**|الكلفة من الراوتر نفسه إلى الجار عبر الواجهة|
|**Advertised Cost**|الكلفة التي يُخبر بها الراوتر جيرانه للوصول إلى شبكة معينة من عنده|
|**Total Path Cost**|مجموع الكلف من الراوتر الحالي إلى الوجهة النهائية (تُحسب بجمع الـAdvertised Costs على طول المسار)|

---

## ⚙️ كيف تُحسب الـAdvertised Cost داخل OSPF؟

كل راوتر:

1. يحسب كلفة الوصول إلى كل شبكة متصلة به مباشرة (بناءً على سرعة الواجهة).
    
2. عندما يرسل **LSA Type 1 (Router LSA)** داخل الـArea، يذكر فيها:
    
    - كل واجهة متصلة.
        
    - والكلفة للوصول إليها.
        
3. الراوترات الأخرى تجمع هذه المعلومات وتشغّل خوارزمية **Dijkstra (SPF)**  
    لتحديد أقل مجموع كلفة (Total Cost) لكل وجهة.
    

---

## 🧩 مثال عملي:

لدينا 3 راوترات متصلة كالتالي:

```
R1 ---[Cost 10]--- R2 ---[Cost 20]--- R3
```

### من وجهة نظر R2:

- الكلفة للوصول إلى R1 = 10
    
- الكلفة للوصول إلى R3 = 20
    

في الـLSA الذي يرسله R2، سيعلن:

```
Advertised Cost to R1 = 10
Advertised Cost to R3 = 20
```

### من وجهة نظر R1:

لكي يصل إلى R3، يجمع الكلف:

```
R1 → R2 = 10
R2 → R3 = 20
----------------
Total Cost = 30
```

👈 هنا الـ **Advertised Cost** الخاصة بـR2 تجاه R3 = 20،  
لكن الـ **Total Path Cost** من R1 إلى R3 = 30.

---

## 🔍 كيف تشوف الـAdvertised Cost في الراوتر؟

في Cisco IOS يمكنك استخدام الأمر:

```
show ip ospf database router
```

ستشاهد تفاصيل الـLSAs المعلنة من كل راوتر داخل المنطقة، وفيها الـAdvertised Cost لكل رابط.

مثال ناتج:

```
Link ID: 10.1.1.2 (Router ID of R2)
Link Data: 10.1.1.2
Metric: 20
```

🔹 هذا الرقم (20) هو الـ**Advertised Cost** التي أعلنها R2 نحو تلك الوجهة.

---

## 📘 ملاحظات مهمة:

1. الـAdvertised Cost **محلية لكل راوتر** — أي أنها تمثل وجهة نظر ذلك الراوتر فقط.
    
2. عندما تُجمع الكلف عبر أكثر من راوتر، ينتج **Total Path Cost** النهائي.
    
3. عند التعديل اليدوي على الـCost في واجهة (`ip ospf cost X`)  
    → تتغير الـAdvertised Cost في الـLSA القادم.
    
4. أي تغيير في الـAdvertised Cost يؤدي إلى **إعادة حساب SPF** في الشبكة.
    

---

## 🧩 مثال واقعي مبسّط:

```
R1 (10.1.1.1) ---[Cost 5]--- R2 (10.1.1.2) ---[Cost 10]--- LAN (10.2.2.0/24)
```

|الراوتر|الإعلان (LSA)|Advertised Cost|
|---|---|---|
|R2|نحو شبكة 10.2.2.0|10|
|R1|نحو نفس الشبكة (بعد الحساب)|5 + 10 = 15|

✅ إذن R1 يعرف أن الوصول إلى شبكة 10.2.2.0 كلفته 15.  
✅ الـ10 هي Advertised Cost من R2.  
✅ الـ5 هي كلفة R1 نفسها للوصول إلى R2.

---

## 🔧 ملخص سريع:

|المصطلح|التعريف|
|---|---|
|**Advertised Cost**|الكلفة التي يعلنها الراوتر للوصول إلى وجهة أو شبكة من وجهة نظره|
|**تُرسل عبر**|LSAs (غالباً Type 1 وType 2)|
|**تُستخدم في**|حساب الـSPF لتحديد أقل كلفة للمسارات|
|**يمكن تعديلها**|بتغيير الـInterface Cost|
|**تؤثر على**|اختيار المسار الأفضل في الـRouting Table|

---

هل ترغب أن أعمل لك **رسم شبكي توضيحي (Diagram)** يبين الفرق بين _Interface Cost_ و _Advertised Cost_ و _Total Path Cost_؟  
يشرح كيف تنتقل القيم بين الراوترات خطوة بخطوة.