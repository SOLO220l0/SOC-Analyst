

---

# ⚙️ **الواجهة الافتراضية في السويتش (Switch Virtual Interface - SVI)**

---

## 📘 أولاً: ما المقصود بـ SVI؟

**SVI** هي اختصار لـ **Switch Virtual Interface**  
أي "الواجهة الافتراضية للسويتش"، وهي **واجهة منطقية** داخل السويتش  
تُستخدم لتوصيل **شبكة VLAN معينة** بطبقة الشبكات (Layer 3).

بعبارة أبسط 👇

> كل VLAN يمكن أن يكون لها **واجهة IP خاصة بها** على السويتش تُعرف باسم **SVI**،  
> وهذه الواجهة تمثل **البوابة الافتراضية (Gateway)** للأجهزة الموجودة داخل تلك الـ VLAN.

---

## 🧩 ثانياً: وظيفة الـ SVI

وظيفة الـ SVI هي **تمكين التواصل بين الـ VLANs (Inter-VLAN Routing)**.  
بدون SVI، الأجهزة داخل VLAN واحدة يمكنها التواصل،  
لكن الأجهزة في VLAN مختلفة **لا تستطيع الاتصال ببعضها** بدون راوتر أو SVI.

---

## 🧠 ثالثاً: مثال لتوضيح الفكرة

لنفترض أن عندنا سويتش من نوع **Layer 3 Switch** فيه ثلاث VLANs:

|VLAN|الاسم|الشبكة|البوابة (SVI IP)|
|---|---|---|---|
|10|HR|192.168.10.0/24|192.168.10.1|
|20|IT|192.168.20.0/24|192.168.20.1|
|30|Finance|192.168.30.0/24|192.168.30.1|

في هذه الحالة، نُنشئ ثلاث واجهات SVI في السويتش كالتالي:

```bash
Switch(config)# interface vlan 10
Switch(config-if)# ip address 192.168.10.1 255.255.255.0
Switch(config-if)# no shutdown

Switch(config)# interface vlan 20
Switch(config-if)# ip address 192.168.20.1 255.255.255.0
Switch(config-if)# no shutdown

Switch(config)# interface vlan 30
Switch(config-if)# ip address 192.168.30.1 255.255.255.0
Switch(config-if)# no shutdown
```

الآن أصبح السويتش قادرًا على:

- التواصل مع كل VLAN.
    
- تمرير البيانات بينها داخليًا بدون الحاجة إلى راوتر خارجي.
    

---

## ⚡ رابعاً: مميزات SVI

1. ✅ تمكّن **الاتصال بين VLANs** داخل السويتش نفسه.
    
2. ✅ تسمح للسويتش أن يكون **بوابة افتراضية (Gateway)** لكل VLAN.
    
3. ✅ تسهّل إدارة الشبكة (يمكنك الوصول إلى السويتش عبر IP).
    
4. ✅ تعمل بسرعة عالية لأن التحويل يتم **داخل العتاد (Hardware Switching)** وليس عبر الراوتر.
    
5. ✅ يمكن ربطها بخصائص أمان مثل **ACLs (Access Control Lists)** للتحكم في حركة المرور بين VLANs.
    

---

## 🔌 خامساً: متطلبات عمل SVI

حتى تعمل SVI بشكل صحيح، يجب توفر ما يلي:

|الشرط|التوضيح|
|---|---|
|1️⃣ السويتش من نوع **Layer 3**|لأن سويتش الطبقة الثانية لا يدعم IP Routing.|
|2️⃣ تم إنشاء VLAN في السويتش|باستخدام الأمر `vlan <رقم>`.|
|3️⃣ تفعيل الـ SVI|باستخدام `no shutdown` داخل واجهة VLAN.|
|4️⃣ وجود منفذ فعّال في الـ VLAN|لأن SVI لا تعمل إذا لم يكن هناك منفذ في تلك الـ VLAN.|
|5️⃣ تفعيل التوجيه (Routing)|بالأمر `ip routing` على السويتش.|

---

## 🧱 سادساً: شكل مبسط للمفهوم

```
[ PC1 ]---(VLAN 10)---[SWITCH]---(VLAN 20)---[ PC2 ]

VLAN 10 → 192.168.10.0/24 → Gateway = 192.168.10.1 (SVI)
VLAN 20 → 192.168.20.0/24 → Gateway = 192.168.20.1 (SVI)
```

السويتش يحتوي على واجهتين افتراضيتين (SVIs)  
يعمل كـ **راوتر داخلي بين VLANs**.

---

## 🧠 سابعاً: الفرق بين SVI و Physical Interface

|المقارنة|SVI|Physical Interface|
|---|---|---|
|النوع|افتراضية|حقيقية (منفذ فعلي)|
|الاستخدام|تمثل VLAN|تربط الأجهزة فعليًا|
|الطبقة|Layer 3|Layer 2|
|العنوان IP|ممكن|أحيانًا غير ممكن|
|الأداء|أسرع داخل السويتش|يعتمد على الربط الخارجي|

---

## 🎯 ثامناً: استخدامات SVI الشائعة

1. **Gateway لكل VLAN** في بيئة الشركات.
    
2. **إدارة السويتش عبر IP** (Management VLAN).
    
3. **ربط شبكات VLAN مختلفة** داخل نفس السويتش.
    
4. **تطبيق سياسات أمنية (ACLs)** للتحكم بين VLANs.
    

---

هل ترغب أن أضيف **رسم توضيحي بالعربي** يبين كيف تعمل الـ SVI  
بين أكثر من VLAN داخل سويتش واحد؟  
سيجعل الفهم البصري للمفهوم أسهل جدًا.

[[65- Redundant Gateway]]
